services:
  mysql:
    image: mysql:8.0
    container_name: mysql8
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: namtran138
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: